# Macros
interface_externa="em0"
interface_interna="em1"
icmp_types = "{ echoreq }"

# Bloqueia tudo
block all
# Libera pacotes de entrada (TCP/22) na interface externa
pass in on $interface_externa proto tcp to port { 22 }
#Libera pacotes ICMP
pass inet proto icmp icmp-type $icmp_types
